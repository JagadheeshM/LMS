<div class="relative flex flex-col justify-center overflow-hidden bg-slate-100 pt-10 sm:py-2 px-12">
    <div class="max-w-screen-lg mx-auto py-4">
        <h2 class="font-bold text-center text-4xl text-slate-700 font-display">
            welcome, <%= user.firstName %>
        </h2> 
    </div>
    <div class="gap-6 flex flex-wrap justify-center pt-6">
        <% if(user.type=="educator"){%>
            <%- include('educatorDash.ejs') %>
        <%}else{%>
            <%- include('learnerDash.ejs') %>
        <%}%>
    </div>
</div>